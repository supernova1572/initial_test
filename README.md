# initial_test
overview
```
  105  sudo upate-alternatives --config java
  106  sudo upate-alternatives
  107  sudo apt install openjdk-11-jdk
  108  sudo apt install openjdk-11
  109  sudo apt-get update
  110  sudo apt-get upgrade
  111  sudo apt-get update
  112  sudo apt-get upgrade
```
